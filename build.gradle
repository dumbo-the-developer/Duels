apply plugin: 'java'
apply plugin: 'maven'

group = 'me.realized'
version = '2.2.3'

description = """Duels"""

sourceCompatibility = 1.7
targetCompatibility = 1.7



repositories {
        
     maven { url "https://hub.spigotmc.org/nexus/content/repositories/snapshots/" }
     maven { url "http://repo.mvdw-software.be/content/groups/public/" }
     maven { url "http://maven.sk89q.com/repo/" }
     maven { url "http://ci.drtshock.net/plugin/repository/everything/" }
     maven { url "http://repo.minelink.net/content/repositories/public/" }
     maven { url "http://repo.maven.apache.org/maven2" }
}
dependencies {
    compile group: 'com.google.code.gson', name: 'gson', version:'2.5'
    compile group: 'be.maximvdw', name: 'MVdWPlaceholderAPI', version:'1.0.2-SNAPSHOT'
    compile(group: 'org.spigotmc', name: 'spigot-api', version:'1.11-R0.1-SNAPSHOT') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.bukkit', name: 'bukkit', version:'1.11-R0.1-SNAPSHOT') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'com.sk89q', name: 'worldguard', version:'6.1.1-SNAPSHOT') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'net.ess3', name: 'EssentialsX', version:'2.0.1') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'com.massivecraft', name: 'Factions', version:'1.6.9.5-U0.1.14') {
exclude(module: 'fanciful')
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'net.minelink', name: 'CombatTagPlus', version:'1.3.0-SNAPSHOT') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
